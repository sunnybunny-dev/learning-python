#Lens function is used to return the number of items (the length of the object)
#Lens in string 
e.g : text = "Hello, World!"
print(len(text))  #output:13

#Lens in lists and tuplets
e.g:fruits = ["apple" , "banana" , "Orange"]
print(len(fruits))  #output:3

#lens in dictionaries (count the number of key values pairs)
e.g: user = {"name": "Alice" , "Age": "25" , "Role": "Admin"}
print(len(user))   #output:3

#lens in sets(counts the unique numbers storeed in the set)
e.g: unique_nmbers = [1,2,2,3,3,3,3]
print(lens(unique_numbers))  #output:3

#Functions